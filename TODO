Fix:
- Message retry or remove. don't allow two user messages at once.
- Add single shot (no tool call, process diffs directly with regex, non agentic)
- Fix grep search, prompts for token efficiency
- Add tool results (diff only) to chat history
- Check multi turn caching
- Lock message input on workflow run and add connected websocket status
- Operational mode check
- Allow multiple tabs open for multiple sessions
- Must focus on last message
- Check partial file truncation and full per file file cut on context size checking
- By some reason, Readme (markdowns dif patching) are not working
- Fix context search folders
- Apply patch should be able analyse if patch was applied correctly and raise if not
- Add option to make diff patch in message history
- On page load, scroll to the end
- Fix calling tools spamming on openai
- Update enums for case consistency
- Cache persists after cleaning history?

--------
Roadmap
- Add monthly usage per model and provider
- Add prompt improvement and versioning based on conversation
- Add comprehensive planning with feature based
- Add multimodality
- Add slash commands
- Add stop run button (stop workflow and reset to previous message state)
- Add MCP Integration
- Add Possibility of rag through semantic search with codeblock chunks similarly to cursor
- Add sub folder project (currently, only top level are projects)
- Order llm's by provider
- Add One tooling and general instructions prompt per provider
- Add compatibility with .CLAUDE .GEMINI .CLINE etc


--------
Bugs
still calling tools to read files already loaded
